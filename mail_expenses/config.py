import os

BASE_DIR = os.path.dirname(os.path.abspath(__file__))

# Папка для временного хранения загруженных файлов (до подтверждения)
UPLOAD_FOLDER = os.path.join(BASE_DIR, 'uploads')

# ─── Папка для хранения файлов чеков по проектам ───────────────────────────
# Внутри этой папки автоматически создаются подпапки по названию проекта.
# Пример структуры: R:\149. БЛИНОВСКАЯ\10. Расходы\Монолит Системз\20260113_Отправка почты ...pdf
STORAGE_FOLDER = r'R:\149. БЛИНОВСКАЯ\10. Расходы'

# ─── Основной Excel-файл на сервере ─────────────────────────────────────────
# При сохранении каждой записи программа добавит новую строку в этот файл.
# Укажите полный путь к файлу.
# Если не нужно — оставьте пустой строкой: EXCEL_MASTER_FILE = ''
EXCEL_MASTER_FILE = r'R:\00_РАБОЧАЯ\20190821_Таблица расходов по проектам.xlsx'

# Название листа в Excel-файле (именно так, как называется вкладка)
EXCEL_MASTER_SHEET = 'Общие расходы'

# ─── Прочие настройки ────────────────────────────────────────────────────────
# Путь к базе данных SQLite (локальная копия всех записей)
DATABASE_PATH = os.path.join(BASE_DIR, 'data', 'expenses.db')

# Разрешённые форматы файлов
ALLOWED_EXTENSIONS = {'pdf', 'png', 'jpg', 'jpeg', 'tiff', 'bmp', 'gif'}

# Максимальный размер файла: 32 МБ
MAX_CONTENT_LENGTH = 32 * 1024 * 1024

# Путь к Tesseract OCR (для Windows)
TESSERACT_PATH = r'C:\Program Files\Tesseract-OCR\tesseract.exe'

# Секретный ключ Flask (измените для безопасности)
SECRET_KEY = 'mail-expenses-local-secret-2026'

# Хост и порт сервера
# HOST = '0.0.0.0' — доступен из локальной сети
# HOST = '127.0.0.1' — только на этом компьютере
HOST = '0.0.0.0'
PORT = 5000
